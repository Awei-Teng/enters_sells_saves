组件化模块
	表格
	树型分类
	导航条
	
开发注意
１.登录超时判断
２.数据为null判断 (需要渲染接口数据时)
３.返回code判断
４.初始化输入框数据(参数需输入时)


每个方法注释功能
禁止用var声明变量，推荐使用es6语法
代码高度复用，禁止出现两块的代码类似。


	/*type传'obj'则为判断json属性是否都不为空
	 * type不传则为判断单个数据是否为空.
	 */
